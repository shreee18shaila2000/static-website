apiVersion: v1
kind: Service
metadata:
  labels:
    app: staticweb
  name: staticweb
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodeport: 30069
  selector:
    app: staticweb
